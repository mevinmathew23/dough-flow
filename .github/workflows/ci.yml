name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  api:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dough_flow_api
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: pip install poetry

      - name: Install dependencies
        run: poetry install

      - name: isort
        run: poetry run isort --check-only api/ tests/

      - name: black
        run: poetry run black --check api/ tests/

      - name: pylint
        run: poetry run pylint --fail-under=9 api/

      - name: mypy
        run: poetry run mypy api/

      - name: bandit
        run: poetry run bandit --recursive --skip B105 api/

      - name: pytest
        run: poetry run pytest tests/unit -q
        env:
          SECRET_KEY: ci-test-secret-key-not-for-production

  ui:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dough_flow_ui
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: dough_flow_ui/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Prettier check
        run: npm run format:check

      - name: Build
        run: npm run build
